// Export all API hooks from a central location
export { useDeals, useDeal } from './useDeals';
export { useNotifications } from './useNotifications';
export { useDashboard } from './useDashboard';

// This allows importing from '@/hooks/api' instead of individual files
// Example: import { useDeals, useNotifications } from '@/hooks/api';
